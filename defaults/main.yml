---
# defaults file for lighthouse-role
#url
lighthouse_git: https://github.com/VKCOM/lighthouse
#dirs
lh_home_dir: "/opt/lighthouse"
#access log name
lh_alog_name: lighthouse_access
#default nginx user
nginx_user: root
#default nginx config file destination
nginx_config_dest: "/etc/nginx/nginx.conf"
